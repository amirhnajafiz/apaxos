package learner

func commit() error {
	// execute blocks in order
	// empty the datastore
	// empty the accepted num and accepted var

	return nil
}

func sync() error {
	// get the clients and balances
	// update the memory

	return nil
}
